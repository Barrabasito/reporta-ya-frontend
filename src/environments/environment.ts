/**
 * Configuración de entorno (producción por defecto).
 * `apiBaseUrl` es la URL base de TU backend; se inyecta vía `API_BASE_URL`.
 */
export const environment = {
  production: true,
  apiBaseUrl: '/api',
};
